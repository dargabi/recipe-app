import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/recipe-app/', // Base path para GitHub Pages - usar el nombre del repositorio
})
